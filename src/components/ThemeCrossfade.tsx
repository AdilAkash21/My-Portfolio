// ─── Theme Crossfade Overlay ───
// No longer needed — transition is handled entirely by ThemeContext's DOM overlay.
// Kept as a no-op for import compatibility.

const ThemeCrossfade = () => null;

export default ThemeCrossfade;
