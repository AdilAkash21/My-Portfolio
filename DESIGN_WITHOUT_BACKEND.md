# Frontend-Only Setup (Design Without Backend)

This guide explains how to take just the design and features of this website without any backend (Supabase/Lovable Cloud) dependencies.

---

## Files to KEEP

### Config Files (project root)
- `index.html`
- `package.json`
- `vite.config.ts`
- `tsconfig.json`
- `tsconfig.app.json`
- `tsconfig.node.json`
- `tailwind.config.ts`
- `postcss.config.js`
- `components.json`
- `eslint.config.js`

### Source Code
- `src/main.tsx`
- `src/App.css`
- `src/index.css`
- `src/vite-env.d.ts`
- `src/lib/utils.ts`
- `src/hooks/use-mobile.tsx`
- `src/hooks/use-toast.ts`
- `src/assets/` — all images
- `src/components/ui/` — all UI components
- `src/contexts/ThemeContext.tsx`

### Design Components (the visual sections)
- `src/components/HeroSection.tsx`
- `src/components/AboutSection.tsx`
- `src/components/SkillsSection.tsx`
- `src/components/ProjectsSection.tsx`
- `src/components/ContactSection.tsx`
- `src/components/Footer.tsx`
- `src/components/Navbar.tsx`
- `src/components/NavLink.tsx`
- `src/components/ThemeToggle.tsx`
- `src/components/ThemeCrossfade.tsx`
- `src/components/ScrollReveal.tsx`
- `src/components/ScrollToTop.tsx`
- `src/components/PageTransition.tsx`
- `src/components/ParallaxBackground.tsx`
- `src/components/LazyImage.tsx`

### Pages
- `src/pages/Index.tsx`
- `src/pages/NotFound.tsx`

### Public Assets
- `public/` — all files

---

## Files to SKIP (Do NOT copy)

| File / Folder | Reason |
|---|---|
| `src/integrations/supabase/` | Backend client and types |
| `src/contexts/AuthContext.tsx` | Uses Supabase authentication |
| `src/pages/Login.tsx` | Requires auth backend |
| `src/pages/Signup.tsx` | Requires auth backend |
| `src/pages/ForgotPassword.tsx` | Requires auth backend |
| `src/pages/ResetPassword.tsx` | Requires auth backend |
| `src/pages/Profile.tsx` | Requires auth backend |
| `src/components/ImageCropDialog.tsx` | Uses Supabase storage |
| `supabase/` | Backend config and migrations |
| `.env` | Backend credentials |
| `node_modules/` | Regenerated by `npm install` |
| `bun.lock` / `package-lock.json` | Regenerated on install |

---

## Code Changes Required

### 1. Clean up `src/App.tsx`

Remove auth imports and routes. Your `App.tsx` should look like this:

```tsx
import { Toaster } from "@/components/ui/toaster";
import { Toaster as Sonner } from "@/components/ui/sonner";
import { TooltipProvider } from "@/components/ui/tooltip";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { BrowserRouter, Routes, Route } from "react-router-dom";
import { ThemeProvider } from "@/contexts/ThemeContext";
import ThemeCrossfade from "@/components/ThemeCrossfade";
import Index from "./pages/Index";
import NotFound from "./pages/NotFound";

const queryClient = new QueryClient();

const App = () => (
  <QueryClientProvider client={queryClient}>
    <ThemeProvider>
      <ThemeCrossfade />
      <TooltipProvider>
        <Toaster />
        <Sonner />
        <BrowserRouter>
          <Routes>
            <Route path="/" element={<Index />} />
            <Route path="*" element={<NotFound />} />
          </Routes>
        </BrowserRouter>
      </TooltipProvider>
    </ThemeProvider>
  </QueryClientProvider>
);

export default App;
```

### 2. Clean up `src/components/ContactSection.tsx`

Remove any `import { supabase }` lines and replace the form submission with a simple alert or integrate a service like [EmailJS](https://www.emailjs.com/) or [Formspree](https://formspree.io/).

Example replacement for the submit handler:

```tsx
const handleSubmit = (e: React.FormEvent) => {
  e.preventDefault();
  // Replace with EmailJS, Formspree, or your own backend
  alert("Message sent! (connect a form service to make this functional)");
};
```

### 3. Clean up `src/components/Navbar.tsx`

Remove any login/signup/profile navigation links or buttons that reference auth pages.

### 4. Remove unused packages (optional)

You can remove `@supabase/supabase-js` from `package.json` since it's no longer needed:

```bash
npm uninstall @supabase/supabase-js
```

---

## How to Run

```bash
npm install
npm run dev
```

App starts at **http://localhost:8080**

> ⚠️ Do NOT use VS Code "Live Server" — use `npm run dev`.

---

## Build for Production

```bash
npm run build
```

Deploy the `dist/` folder to any static host (Vercel, Netlify, Cloudflare Pages, GitHub Pages, etc.).
